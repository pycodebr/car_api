apiVersion: v1
kind: Service
metadata:
  name: {{ include "car-api.fullname" . }}
  labels:
    {{- include "car-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "car-api.selectorLabels" . | nindent 4 }}
